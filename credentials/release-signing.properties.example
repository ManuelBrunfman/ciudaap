# Copy to credentials/release-signing.properties and fill with your values.
# Ensure the referenced keystore file exists and is ignored by Git.

# Path to your release keystore (relative to project root)
storeFile=credentials/release.keystore

# Keystore password
storePassword=CHANGE_ME

# Key alias
keyAlias=upload

# Key password
keyPassword=CHANGE_ME
