// RUTA: android/settings.gradle

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
    plugins {
        id("org.gradle.kotlin.kotlin-dsl") version "4.0.5"
    }
}

include ':app'
